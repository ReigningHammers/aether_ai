# Architecture

- **Local-first**: Runs on your hardware (Windows + RTX)
- **Docker**: Easy prod-like
- **Hybrid DB**: Postgres (SQL) + Redis (cache) + pgvector (semantic)
- **FastAPI**: All skills exposed uniformly

Windows Notes: PowerShell + Docker Desktop recommended.